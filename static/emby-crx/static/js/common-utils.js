// Emby CRX 通用工具函数
// 此文件为占位符，用于解决构建问题

// 通用工具函数
const CommonUtils = {
    // 工具函数占位符
    init: function() {
        console.log('CommonUtils 已初始化');
    }
};

// 导出模块
if (typeof module !== 'undefined' && module.exports) {
    module.exports = CommonUtils;
}