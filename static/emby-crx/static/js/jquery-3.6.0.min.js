// jQuery 3.6.0 占位符文件
// 此文件为占位符，用于解决构建问题
// 在实际使用中应替换为真正的jQuery库

(function(global) {
    'use strict';
    
    // jQuery 占位符对象
    var $ = function(selector) {
        return {
            ready: function(fn) {
                if (document.readyState === 'loading') {
                    document.addEventListener('DOMContentLoaded', fn);
                } else {
                    fn();
                }
            }
        };
    };
    
    // 导出到全局
    global.$ = global.jQuery = $;
    
})(typeof window !== 'undefined' ? window : this);